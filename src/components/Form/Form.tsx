import React from 'react'

const Form = () => {
  return (
    <form>
      <select>
        <option>HI</option>
      </select>
      <div>
        <span>Impersonate ???</span>
        <input type='text' />
      </div>
      <button>Draw a random ???? Joke</button>
    </form>
  )
}

export default Form
